"""Auth endpoints — implemented in A-PR2."""

from fastapi import APIRouter

router = APIRouter()


# TODO (A-PR2): POST /signup, POST /login, GET /me
# Acceptance: signup → login → /auth/me with token → returns user; tests green
