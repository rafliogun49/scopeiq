"""Synthesizer agent — implemented in B-PR4.

Writes the final 4-section Markdown report from pgvector corpus via rag_query.
Uses gpt-4o (the one premium-model agent — quality matters here).
Hard rule: every quantitative claim cites [source: <url>].
See PRD §10 and TEAM_SPLIT §4 (B-PR4).
"""
# TODO (B-PR4): define synthesizer Agent using openai-agents SDK
